module = angular.module("walapp", ["ngRoute", "ngResource"])

module.controller("MainCtrl", ["$scope", ($scope) ->

])

module.config ["$routeProvider", ($routeProvider) ->
  $routeProvider.when("/",
    templateUrl: "<%= asset_path('index.html.haml') %>"
    controller: "MainCtrl"
  ).otherwise(redirectTo: "/")
]
